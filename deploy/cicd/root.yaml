apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: root-cicd-gitops-infra
  namespace: argocd
spec:
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  project: mgmt
  source:
    directory:
      recurse: true
    path: deploy/cicd
    repoURL: git@github.com:nexla/gitops-infra-apps.git
    targetRevision: HEAD
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - RespectIgnoreDifferences=true
      - Prune=false
